@foreach (var item in Model)
{
<div class="col-sm-6 col-xl-3">
    <div class="box">
        <a href="@Url.Action("xemchitiet", new {@Masp=item.ma_SP })">
            <div class="img-box">
                <img src=@Url.Content("~/Images/" + item.link_anhdaidien) />
            </div>
            <div class="detail-box">
                <h6>
                    @item.ten_SP
                </h6>
                <h6>
                    Giá:
                    <span>
                        @item.dongiaban
                    </span>
                </h6>
            </div>
            <div class="new">
                <span>
                    @item.mota
                </span>
            </div>
        </a>
    </div>
</div>}

